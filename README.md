# Stremio 3D Only Filter v3

Fixes:
- Personalized `/configure` route now works.
- Personalized manifest is no longer marked as configurable, so Stremio shows **Install** instead of only **Configure**.
- Existing configured links can still be reopened and edited.

Update your existing GitHub/Vercel project by replacing:
- `api/index.js`
- `package.json`
- `vercel.json`
- `README.md`

Commit the changes and wait for Vercel to redeploy.
